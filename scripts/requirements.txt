# Translation tooling dev dependencies. Install:
#   pip install -r client/scripts/requirements.txt
#
# Used by translate-legal.py + translate-i18n.py + translate-i18n-anthropic.py
# only. Other scripts in client/scripts/ are JavaScript (Node) and need
# nothing from here.
httpx>=0.27,<1.0
pyyaml>=6.0
opencc-python-reimplemented>=0.1.7
# Anthropic SDK powers translate-i18n-anthropic.py — the capacity-fallback
# adapter used when the B200 / Gemma sidecar is unavailable. Pinned to
# the latest stable as of 2026-04-26 (claude-opus-4-7 generation).
anthropic>=0.40,<1.0
